/**
 * Re-exports from the real OpenClaw SDK.
 * Previously these were hand-written shims; now we import the actual types.
 */
export type { OpenClawPluginApi, PluginLogger } from 'openclaw/plugin-sdk/plugin-entry';
